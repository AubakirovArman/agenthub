# AgentHub PRD v4 Backlog

## Current Queue

- Current: continue hardening the local release path toward 1.0.

## Remaining Tasks

- Continue collecting multi-day real-project dogfood with `target/dogfood/history/` and pass `scripts/dogfood-readiness.sh --check` before the final 1.0 tag.
- Verify the GitHub Pages deployment and publish the wiki seed with `scripts/publish-wiki.sh`.
- Publish Homebrew tap, Scoop bucket, and winget submission after 1.0 release assets are final.
