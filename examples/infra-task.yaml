task:
  id: generate_infra_plan_note
  type: infra.command
  title: Generate an infra plan note

workspace:
  type: infra.git
  isolation: git_worktree

execution:
  commands:
    - mkdir -p infra/plans
    - "printf 'plan: placeholder\\nrisk: none\\n' > infra/plans/plan.yaml"

scope:
  allow:
    - infra/**
  deny:
    - .agent/**

verify:
  profile: infra_plan
  commands:
    - test -s infra/plans/plan.yaml
    - grep -q 'plan:' infra/plans/plan.yaml

transaction:
  commit_on_success: true
  memory_promotion: on_success
  diff_limits:
    max_files_changed: 3
    max_lines_added: 80
    max_lines_deleted: 20
